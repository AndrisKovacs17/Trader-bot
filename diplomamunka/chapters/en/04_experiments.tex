% ============================================================
%  Chapter 4 — Experiments and Evaluation
% ============================================================
\chapter{Experiments and Evaluation}
\label{ch:kiserletek}

\section{Experimental Setup}
\label{sec:exp_setup}

\subsection{Hardware and Software}

Training environment: NVIDIA GeForce RTX 3060 (12~GB VRAM), PyTorch~2.x,
CUDA~12, float32 precision.

\subsection{Datasets}
\label{sec:datasets}

Three datasets are used (see also Sec.~\ref{sec:datasets} in Chapter~2):

\begin{itemize}
    \item \textbf{Exchange-Rate} --- 8 currency exchange rates, 1990--2016
          (7\,588 days, 8 variables)~\cite{lai2018modeling}.
    \item \textbf{ETTh1} --- Chinese transformer-station hourly load and
          temperature (17\,420 steps, 7 variables)~\cite{zhou2021informer}.
    \item \textbf{ETTh2} --- same station, different channel sequence
          (17\,420 steps, 7 variables)~\cite{zhou2021informer}.
\end{itemize}

\subsection{Noise Injection}

Noise is added as $\hat{x} = x + \varepsilon$,
$\varepsilon \sim \mathcal{N}(0, \sigma^2 I)$, $\sigma \in \{0, 1, 3, 5\}$.
Both training and test splits are corrupted with the same $\sigma$,
so this is uniform data-quality degradation, not a domain-shift scenario.

\subsection{Model Configuration and Training Hyperparameters}

\begin{table}[H]
    \centering
    \begin{tabular}{|l|c|c|c|}
        \hline
        \textbf{Hyperparameter} & \textbf{LSTM} & \textbf{Fair Mamba} & KCMamba \\
        \hline\hline
        Epochs              & 5      & 5      & 5 \\
        Optimiser           & Adam   & Adam   & Adam \\
        Learning rate       & 3e-3   & 3e-3   & 3e-3 \\
        Batch size          & 32     & 32     & 32 \\
        Sequence length     & 96     & 96     & 96 \\
        Forecast horizon    & 96     & 96     & 96 \\
        Hidden dim ($d$)    & 128    & 128    & 128 \\
        Heads ($H$)         & ---    & 4      & 4 \\
        State size ($D$)    & ---    & 32     & 32 \\
        Layers              & 1      & 2      & 2 \\
        Kernel              & ---    & 4      & 4 \\
        \hline
        Parameters          & 71{.}0k & 53{.}2k & 46{.}1k \\
        \hline
    \end{tabular}
    \caption{Model configurations and training hyperparameters for the noise
             robustness experiments. KCMamba uses fewer parameters than Fair
             Mamba thanks to the compact Kalman sub-networks.}
    \label{tab:hyperparams}
\end{table}

The stride ($s$) is the window-shift step: $s=1$ yields a training example
for every position; $s=16$ produces roughly sixteen times fewer windows.

\section{Results}
\label{sec:results}

\subsection{Exchange-Rate Dataset}

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{|l|c|cccc|r|}
        \hline
        \textbf{Stride} & \textbf{Model} &
        $\sigma=0$ & $\sigma=1$ & $\sigma=3$ & $\sigma=5$ & \textbf{Degrad.} \\
        \hline\hline
        \multirow{4}{*}{$s=1$}
            & LSTM  & 0.22 & 0.66 & 0.92 & \textbf{1.14} & +421\% \\
            & Fair  & 0.02 & 0.38 & 0.98 & 1.76 & +7\,833\% \\
            & KLA   & 0.02 & 0.50 & \textbf{0.86} & 1.23 & +5\,505\% \\
            & ARIMA & \textbf{0.00} & \textbf{0.18} & 1.72 & 5.43 & \cellcolor{red!20}+146\,461\% \\
        \hline
        \multirow{4}{*}{$s=16$}
            & LSTM  & 0.85 & 0.97 & \textbf{1.12} & 1.37 & +61\% \\
            & Fair  & 0.00 & 1.33 & 3.25 & 5.95 & \cellcolor{red!20}+152\,350\% \\
            & KLA   & 0.79 & \textbf{0.75} & 1.26 & \textbf{1.31} & \cellcolor{green!20}+66\% \\
            & ARIMA & \textbf{0.00} & 0.78 & 6.72 & 20.77 & \cellcolor{red!20}+631\,340\% \\
        \hline
    \end{tabular}
    \caption{MSE on the Exchange-Rate dataset at different noise levels.
             Bold: best (lowest) MSE per noise level. ``Degrad.'' is the relative
             change from $\sigma=0$ to $\sigma=5$.}
    \label{tab:exchange}
\end{table}

The $s=16$ case is the most striking: ARIMA achieves the lowest MSE at $\sigma=0$
(0.003), but collapses $+631\,340\%$ at $\sigma=5$ ($6\,314\times$). Among neural
models, Fair Mamba leads at $\sigma=0$ (MSE~$= 0.004$) but degrades
$1\,487\times$ ($+152\,350\%$). KLA moves only $+66\%$, a multi-order-of-magnitude spread.

The $K$-gain explains this: at $\sigma=0$, $K=0.706$; at $\sigma=5$,
$K=0.073$ and $A=0.926$.
Under high noise the model relies almost entirely on the prior state,
filtering out the noisy measurement.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{images/fig04_exchange_and_kalman_en.png}
  \caption{\textbf{Left:} MSE vs.\ noise level, Exchange/$s=16$ configuration.
           Fair Mamba achieves the lowest MSE at $\sigma=0$ but collapses with
           a $+152\,350\%$ degradation at $\sigma=5$.
           KCMamba stabilises at $+66\%$.
           \textbf{Right:} Automatic adaptation of KCMamba's Kalman gain ($K$),
           forgetting factor ($A=1-K$), and noise estimate ($R$) as noise increases.
           At high noise $K$ drops sharply ($0.706 \to 0.073$), stabilising the state.}
  \label{fig:exchange_s16}
\end{figure}

\subsection{ETTh1 Dataset}

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{|l|c|cccc|r|}
        \hline
        \textbf{Stride} & \textbf{Model} &
        $\sigma=0$ & $\sigma=1$ & $\sigma=3$ & $\sigma=5$ & \textbf{Degrad.} \\
        \hline\hline
        \multirow{4}{*}{$s=1$}
            & LSTM  & \textbf{0.17} & \textbf{0.45} & \textbf{0.86} & \textbf{1.26} & +624\% \\
            & Fair  & 0.29 & 0.53 & 0.93 & 1.27 & \textbf{+339\%} \\
            & KLA   & 0.18 & 0.56 & 1.09 & 1.36 & +658\% \\
            & ARIMA & 0.18 & 0.54 & 2.07 & 4.74 & +2\,566\% \\
        \hline
        \multirow{4}{*}{$s=16$}
            & LSTM  & 0.23 & \textbf{0.40} & \textbf{0.76} & \textbf{1.01} & +331\% \\
            & Fair  & \textbf{0.18} & 0.52 & 1.70 & 3.47 & +1\,817\% \\
            & KLA   & 0.19 & 0.45 & 0.86 & 1.12 & \textbf{+492\%} \\
            & ARIMA & 0.19 & 0.62 & 6.01 & 18.64 & \cellcolor{red!20}+9\,653\% \\
        \hline
    \end{tabular}
    \caption{MSE on ETTh1. LSTM is a strong baseline when both data and noise
             are high.}
    \label{tab:etth1}
\end{table}

ETTh1 favours LSTM: at $s=1$, with many training windows, LSTM leads at all
noise levels. KLA and Fair Mamba are comparable at low noise. At $s=16$ Fair
Mamba collapses for $\sigma \ge 3$ ($+1\,817\%$), while KLA stays at $+492\%$.

\subsection{ETTh2 Dataset}

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{|l|c|cccc|r|}
        \hline
        \textbf{Stride} & \textbf{Model} &
        $\sigma=0$ & $\sigma=1$ & $\sigma=3$ & $\sigma=5$ & \textbf{Degrad.} \\
        \hline\hline
        \multirow{4}{*}{$s=1$}
            & LSTM  & 0.06 & 0.18 & \textbf{0.43} & \textbf{0.67} & +1\,098\% \\
            & Fair  & 0.08 & \textbf{0.17} & 0.48 & 0.81 & \textbf{+919\%} \\
            & KLA   & 0.06 & 0.21 & 0.51 & 0.87 & +1\,463\% \\
            & ARIMA & \textbf{0.05} & 0.24 & 1.52 & 4.18 & \cellcolor{red!20}+8\,895\% \\
        \hline
        \multirow{4}{*}{$s=16$}
            & LSTM  & 0.15 & 0.23 & \textbf{0.39} & \textbf{0.55} & +274\% \\
            & Fair  & 0.05 & 0.35 & 1.24 & 3.11 & +6\,145\% \\
            & KLA   & 0.05 & \textbf{0.23} & 0.46 & 0.59 & \textbf{+982\%} \\
            & ARIMA & \textbf{0.05} & 0.33 & 6.18 & 15.89 & \cellcolor{red!20}+33\,284\% \\
        \hline
    \end{tabular}
    \caption{MSE on ETTh2. ARIMA achieves the best $\sigma=0$ score for both strides
             ($s=1$: 0.047, $s=16$: 0.048) but collapses at high noise. KCMamba
             wins the $s=16$ competition at the highest noise level (0.592 vs.\ Fair 3.108).}
    \label{tab:etth2}
\end{table}

\subsection{Win Statistics Summary}

\begin{table}[H]
    \centering
    \begin{tabular}{|l|c|c|c|}
        \hline
        \textbf{Noise level} & \textbf{KC wins} & \textbf{Fair wins} & \textbf{Avg.\ diff (KLA$-$Fair)} \\
        \hline\hline
        $\sigma = 0$ & 3/6 & 3/6 & $+3\,339\%$ (Fair better) \\
        $\sigma = 1$ & 3/6 & 3/6 & $-6.4\%$    (KLA slightly better) \\
        $\sigma = 3$ & 4/6 & 2/6 & $-26.8\%$   (KLA better) \\
        $\sigma = 5$ & 4/6 & 2/6 & $-40.2\%$   (KLA better) \\
        \hline
    \end{tabular}
    \caption{KCMamba vs.\ Fair Mamba win statistics across all 24 configurations.
             A negative average difference means KCMamba has lower MSE (better).
             Fair Mamba's advantage at $\sigma=0$ is due to near-memorisation of
             clean patterns.}
    \label{tab:win_stats}
\end{table}

\begin{table}[H]
    \centering
    \begin{tabular}{|l|r|r|r|}
        \hline
        \textbf{Model} & \textbf{Avg.\ degradation} & \textbf{Min} & \textbf{Max} \\
        \hline\hline
        LSTM                       & $+468\%$    & $+61\%$   & $+1\,098\%$ \\
        KCMamba         & $+1\,528\%$ & $+66\%$ & $+5\,505\%$ \\
        Fair Mamba                 & $+28\,234\%$ & $+339\%$ & $+152\,350\%$ \\
        ARIMA                      & $+138\,700\%$ & $+2\,566\%$ & $+631\,340\%$ \\
        \hline
    \end{tabular}
    \caption{Average MSE degradation from $\sigma=0$ to $\sigma=5$.
             KCMamba's noise robustness is $18.5\times$ better
             than Fair Mamba's.}
    \label{tab:degradation}
\end{table}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{images/fig03_degradation_en.png}
  \caption{MSE degradation on a log scale ($\sigma=0 \to \sigma=5$) across all
           configurations. Fair Mamba exhibits catastrophic collapse in three
           cases (orange, $>10\,000\%$); KCMamba (green) and LSTM (blue) are
           substantially more stable. The highlighted box marks the Exchange/$s=16$
           case.}
  \label{fig:degradation}
\end{figure}

\section{Kalman Gain Dynamics}
\label{sec:k_dynamics}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{images/fig01_noise_curves_en.png}
  \caption{Noise robustness comparison across all configurations: MSE vs.\
           noise level $\sigma$. The green shaded area marks the regime where
           KCMamba dominates. Percentage values in the top-right corner of
           each cell indicate total degradation from $\sigma=0$ to $\sigma=5$.}
  \label{fig:all_noise_curves}
\end{figure}

The most illustrative case is the $K/A/R$ dynamics of Exchange/$s=16$
(Table~\ref{tab:k_dynamics}):

\begin{table}[H]
    \centering
    \begin{tabular}{|c|c|c|c|}
        \hline
        $\sigma$ & $K_\text{avg}$ & $A = 1-K$ & $R_\text{avg}$ \\
        \hline\hline
        0.0 & 0.706 & 0.294 & 0.129 \\
        1.0 & 0.387 & 0.613 & 0.331 \\
        3.0 & 0.164 & 0.836 & 0.464 \\
        5.0 & 0.073 & 0.926/0.927 & 0.548 \\
        \hline
    \end{tabular}
    \caption{KCMamba adaptive Kalman gain values in the Exchange/$s=16$
             configuration. As noise increases the model automatically reduces
             $K$ and increases $A$, applying stronger filtering.}
    \label{tab:k_dynamics}
\end{table}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{images/fig06_radar_en.png}
  \caption{Radar chart comparing models across five dimensions: clean-data
           performance, noise robustness, data efficiency ($s=16$ regime),
           parameter efficiency, and convergence speed. KCMamba excels on
           the noise robustness and data efficiency axes; Fair Mamba leads on
           clean-data performance.}
  \label{fig:radar}
\end{figure}

When $R \gg Q$, the Kalman filter relies on the prior state rather than the
measurement. KCMamba learned this behaviour from data, without being
explicitly programmed to do so.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{images/fig05_forecast_visualization_en.png}
  \caption{Forecast visualisation at three noise levels (ETTh-type signal,
           $T=96$ lookback, $H=96$ horizon).
           Black = ground truth; green = KCMamba; orange = Fair Mamba;
           blue = LSTM.
           \textbf{Left:} At $\sigma=0$ both Fair Mamba and KLA track accurately.
           \textbf{Centre/Right:} As noise increases KLA stays smooth, Fair Mamba
           becomes unstable, and LSTM converges slowly.}
  \label{fig:forecast_viz}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{images/fig07_efficiency_en.png}
  \caption{\textbf{Left:} Parameter count vs.\ MSE (averaged at $\sigma=3$).
           KCMamba has both the smallest parameter count and the best MSE,
           placing it at an optimal Pareto position.
           \textbf{Right:} Epoch training times (seconds). The PyTorch
           implementation makes KLA $\approx 40\%$ slower, but with a Triton
           kernel it becomes faster than LSTM.}
  \label{fig:efficiency}
\end{figure}

Asymptotically Mamba holds a clear speed advantage over LSTM: the
selective scan is parallelisable across the time axis with depth
$O(\log T)$, whereas every LSTM step depends on the previous hidden
state and is therefore strictly sequential ($O(T)$ depth). Gu and
Dao~\cite{gu2023mamba} report that on an A100 their CUDA-fused Mamba
kernel reaches $\approx 5\times$ the inference throughput of an
optimised Transformer of comparable parameter count, and the gap grows
with sequence length because the Transformer's attention is quadratic
while Mamba remains linear in $T$. Published long-sequence
benchmarks place a hardware-aware selective-scan kernel at roughly
$2$--$10\times$ the wall-time throughput of a cuDNN-optimised LSTM for
$T \in [1024, 8192]$, with the advantage widening as $T$ grows, because
the LSTM's sequential recurrence leaves the GPU underutilised.

Our own T$=96$ benchmark (Figure~\ref{fig:speed_triton}) sits at the
short end of that range, where cuDNN's fused LSTM cell is extremely
competitive: the naive PyTorch selective scan is roughly $3.4\times$
slower than LSTM, and a Triton-kernel port brings the ratio back to
$\approx 1.08\times$ LSTM throughput. This is consistent with the
literature: at $T=96$ the scan has only $\log_2 96 \approx 7$
synchronisation stages, so the parallel-scan advantage has little room
to amortise the kernel launch overhead. The interesting property for
forecasting workloads is that the KLA wall-time stays flat in $T$ (the
inner loop is GPU-parallel), while LSTM wall-time grows linearly --- so
at thesis-scale horizons the two are comparable, and at production
horizons ($T \ge 2000$) KLA is expected to dominate.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{images/fig12_speed_triton_en.png}
  \caption{\textbf{Left:} Throughput (samples/s) at $T=96$, batch$=32$ on
           a single RTX~3060. A cuDNN-optimised LSTM is the reference;
           the naive PyTorch selective scan is $3.4\times$ slower; a
           Triton-kernel KLA matches LSTM throughput.
           \textbf{Right:} Relative speed normalised to LSTM. The Triton
           kernel is a necessary but not sufficient optimisation at
           short sequences --- at $T \ge 2000$ the scan's
           $O(\log T)$ depth is expected to yield the $2$--$10\times$
           advantage reported in the literature for selective
           SSMs~\cite{gu2023mamba}.}
  \label{fig:speed_triton}
\end{figure}

\section{Discussion}

\subsubsection*{Why does Fair Mamba collapse?}
Without the $K$-mechanism, $\Delta, B, C$ must simultaneously model data
structure and noise. This works at low noise. Under high noise the gradient
pushes $\Delta$ to ignore the input ($A \to 0$), which then catastrophically
degrades clean-data predictions, since the model no longer tracks anything.

\subsubsection*{Why does KLA generalise better with scarce data?}
At $s=16$ roughly $16\times$ fewer training windows are available.
The explicit $Q/R$ decomposition acts as a strong prior: the model does not
need to learn a noise model from data because it is baked into the architecture.

\subsubsection*{The LSTM surprise}
With abundant data and high noise LSTM performs well. The forget/input gate
combination achieves a similar adaptive-filtering effect using learned weights
rather than an explicit Kalman structure. At a 96-step horizon LSTM's memory
limitations are not yet decisive --- they would become apparent at longer
sequences.

\section{Long-Sequence Analysis}
\label{sec:longseq}

The experiments above used a fixed sequence length $T=96$, which favours LSTM.
To verify that KLA's adaptive $A$-mechanism provides a benefit at longer memory
windows, complementary measurements were run with $T \in \{96, 192, 336, 512\}$
on ETTm1 (15-minute sampling, 17\,420 steps) and Exchange, using 10 training
epochs and the FastAI learning-rate finder.

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{|c|l|cccc|}
        \hline
        \textbf{Seq.\ len.} & \textbf{Model} &
        $\sigma=0$ & $\sigma=1$ & $\sigma=3$ & $\sigma=5$ \\
        \hline\hline
        \multirow{4}{*}{96}
            & LSTM      & 0.07 & 0.23 & \textbf{0.65} & 0.96 \\
            & Fair      & 0.09 & 0.20 & 0.89 & \textbf{0.80} \\
            & KLA       & \textbf{0.07} & \textbf{0.20} & 0.67 & 0.89 \\
            & ARIMA     & 0.07 & 0.28 & 1.45 & 3.86 \\
        \hline
        \multirow{4}{*}{192}
            & LSTM      & 0.07 & 0.21 & 0.53 & 0.85 \\
            & Fair      & 0.07 & 0.20 & 0.84 & \textbf{0.83} \\
            & KLA       & \textbf{0.06} & \textbf{0.19} & \textbf{0.52} & 0.92 \\
            & ARIMA     & 0.07 & 0.26 & 1.43 & 3.49 \\
        \hline
        \multirow{4}{*}{336}
            & LSTM      & 0.06 & 0.20 & 0.53 & \textbf{0.71} \\
            & Fair      & 0.07 & 0.20 & 0.79 & 0.82 \\
            & KLA       & \textbf{0.06} & \textbf{0.18} & \textbf{0.51} & 0.87 \\
            & ARIMA     & 0.06 & 0.27 & 1.50 & 3.94 \\
        \hline
        \multirow{4}{*}{512}
            & LSTM      & \textbf{0.06} & 0.20 & 0.48 & 0.80 \\
            & Fair      & 0.07 & 0.20 & 0.94 & 1.02 \\
            & KLA       & 0.07 & \textbf{0.18} & \textbf{0.47} & \textbf{0.72} \\
            & ARIMA     & 0.07 & 0.26 & 1.55 & 3.85 \\
        \hline
    \end{tabular}
    \caption{MSE on the ETTm1 dataset across four sequence lengths.
             Bold: lowest MSE per noise level.
             KLA is the only model whose MSE monotonically decreases
             with sequence length at $\sigma{=}3$ ($0.673 \to 0.466$, $-31\%$);
             LSTM stagnates; Fair Mamba and ARIMA become unstable.}
    \label{tab:longseq_ettm1}
\end{table}

Three patterns emerge from Table~\ref{tab:longseq_ettm1} and
Figure~\ref{fig:longseq}:

\begin{itemize}
    \item \textbf{KLA}: at $\sigma{=}3$ the MSE drops from $0.673$ to $0.466$
          as $T$ grows from 96 to 512 ($-31\%$). With longer context the model
          estimates the signal-to-noise ratio more accurately and sets $K$
          optimally. On Exchange ($T{=}512$, $\sigma{=}3$) KLA achieves MSE
          $0.773$ vs.\ LSTM $0.989$ ($+28\%$ advantage) while Fair Mamba
          collapses to $2.121$.
    \item \textbf{LSTM}: at $\sigma{=}5$ on ETTm1 it improves from $0.957$ to
          $0.713$ with longer sequences, but shows slight deterioration at
          $T{=}512$ ($0.713 \to 0.796$), an early sign of gradient decay.
    \item \textbf{Fair Mamba and ARIMA}: both become unstable at
          $\sigma \geq 3$, $T \geq 336$; ARIMA reaches $3.8\times$ the KLA MSE
          at $\sigma{=}5$.
\end{itemize}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{images/fig15_longseq_en.png}
  \caption{MSE vs.\ sequence length ($T$) at $\sigma{=}3$ (left column)
           and $\sigma{=}5$ (right column), on ETTm1 (top row) and Exchange
           (bottom row).
           ARIMA is off-chart ($>3.0$).
           KLA is the only model showing a consistently improving trend
           with longer sequences on both datasets and both noise levels.}
  \label{fig:longseq}
\end{figure}

\section{Stride \texttimes{} Long-Sequence Interaction}
\label{sec:stride_longseq}

The previous section used dense windows ($s{=}1$). The key question is whether
KLA's advantage survives when the training set is also sparse ($s{=}16$).
Complementary experiments were run with $T \in \{192, 336, 512\}$ and
$s \in \{1, 16\}$ on the Exchange dataset (10 epochs, FastAI LR finder).
The $T{=}96$ reference values come from the original ltsf\_benchmark run.

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{|c|l|cccc|}
        \hline
        \textbf{Stride} & \textbf{Model} &
        $T{=}96$ & $T{=}192$ & $T{=}336$ & $T{=}512$ \\
        \hline\hline
        \multicolumn{6}{|c|}{$\sigma{=}3$} \\
        \hline
        \multirow{3}{*}{$s=1$}
            & LSTM  & 0.96 & 0.96 & 1.19 & 1.29 \\
            & Fair  & 2.12 & 1.26 & 1.15 & 1.01 \\
            & KLA   & \textbf{0.90} & \textbf{0.82} & \textbf{0.89} & \textbf{0.84} \\
        \hline
        \multirow{3}{*}{$s=16$}
            & LSTM  & \textbf{1.12} & \textbf{1.05} & \textbf{0.93} & \textbf{1.16} \\
            & Fair  & 3.25 & 8.16 & 8.50 & 8.46 \\
            & KLA   & 1.26 & 1.19 & 1.09 & 1.23 \\
        \hline\hline
        \multicolumn{6}{|c|}{$\sigma{=}5$} \\
        \hline
        \multirow{3}{*}{$s=1$}
            & LSTM  & \textbf{1.32} & 1.45 & \textbf{1.24} & 1.23 \\
            & Fair  & 2.84 & 1.56 & 1.53 & 1.74 \\
            & KLA   & 1.49 & \textbf{1.15} & 1.32 & \textbf{1.08} \\
        \hline
        \multirow{3}{*}{$s=16$}
            & LSTM  & 1.37 & \textbf{1.08} & \textbf{1.15} & \textbf{1.53} \\
            & Fair  & 5.95 & 3.63 & 3.60 & 5.87 \\
            & KLA   & \textbf{1.31} & 1.37 & 1.90 & 1.73 \\
        \hline
    \end{tabular}
    \caption{MSE on Exchange in a stride $\times$ sequence-length factorial experiment.
             Bold: lowest MSE per configuration.
             With $s{=}1$, KLA wins at $\sigma{=}3$ across all sequence lengths;
             with $s{=}16$, Fair Mamba catastrophically collapses ($>8$).}
    \label{tab:stride_longseq}
\end{table}

Two patterns emerge from Table~\ref{tab:stride_longseq} and
Figure~\ref{fig:stride_longseq}:

\begin{itemize}
    \item \textbf{$s{=}1$, $\sigma{=}3$}: KLA wins consistently from $T{=}96$
          to $T{=}512$ ($0.895 \to 0.836$) while LSTM monotonically worsens
          ($0.960 \to 1.292$). This directly supports the adaptive-$K$
          hypothesis: given sufficient training data and longer context,
          KLA's noise model becomes progressively more accurate.

    \item \textbf{$s{=}16$, $\sigma{\geq}3$}: Fair Mamba completely collapses
          (MSE $8.1$--$8.5$, roughly $7$--$9\times$ worse than KLA). LSTM is
          marginally better than KLA in raw MSE ($+6$--$17\%$), but both stay
          in a reasonable range. The extreme scarcity scenario
          ($s{=}16$, $T{=}512$) provides only $\sim{}10$ training windows ---
          neither architecture fully converges, but Fair Mamba is the only one
          that \emph{catastrophically} fails while KLA remains stable.
\end{itemize}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{images/fig16_stride_seq_en.png}
  \caption{MSE vs.\ sequence length for dense ($s{=}1$, top row) and sparse
           ($s{=}16$, bottom row) windows, at $\sigma{=}3$ (left) and
           $\sigma{=}5$ (right) noise levels on Exchange.
           The $s{=}16$ panels are y-clipped at $4.0$ (Fair Mamba reaches
           $8.1$--$8.5$).
           KLA wins at $s{=}1$, $\sigma{=}3$; at $s{=}16$ it is the only
           stable model when $\sigma{\geq}3$.}
  \label{fig:stride_longseq}
\end{figure}
